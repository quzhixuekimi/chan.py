"""
trade_system package (scaffold)

占位包：只包含空模块和包初始化文件，用于承载 paper-trading 相关模块的骨架。

注意：该包不修改或覆盖现有仓库中的任何文件。要使用本包中的模块，请确保从仓库根目录运行脚本，
这样 Python 的模块搜索路径（sys.path）会包含项目根目录，能够直接导入顶层模块 `Chan`，例如：

    from Chan import CChan

如果你的运行环境不是从仓库根目录启动，请将项目根目录加入 PYTHONPATH 或在运行时修改 sys.path。
"""

__all__ = [
  # 子包
]
